# Railway Configuration for SparkNet API
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd fire-pred-mvp-main && python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "sparknet-api"

[services.env]
PORT = "8000"
